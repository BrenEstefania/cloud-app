version: "3.8"

services:
  api:
    build: ./api-service
    ports:
      - "5001:3000"   
    container_name: api-service

  web:
    build: ./web-client
    ports:
      - "3001:80"
    container_name: web-client
    depends_on:
      - api